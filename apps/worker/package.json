{
  "name": "@clemens/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "db:migrate": "wrangler d1 execute clemens-db --file=./migrations/0001_init.sql"
  },
  "dependencies": {
    "hono": "^4.9.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260115.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.33.1"
  }
}
